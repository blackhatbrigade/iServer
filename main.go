package main

func main() {

	// Create the ReadModel Facade

	//uri := "mongodb://127.0.0.1"
	//ctx, cancel := context.WithCancel(context.Background())
	//defer cancel()

	//client := NewClient(ctx, uri)

	//collection := client.client.Database("golang").Collection("messages")

	// Create Read model generators.

	// Create Event Bus
	// Add handlers to Event Bus

	// Create Event Repository

	// Create Command Handler

	// Create Dispatcher
	// Register Command Handlers with Dispatcher

	//res, err := collection.InsertOne(ctx, bson.M{"GlobalPosition": "1"})

	//if err != nil {
	//panic(err)
	//}

	//id := res.InsertedID
	//fmt.Println("Input row", id)
}
